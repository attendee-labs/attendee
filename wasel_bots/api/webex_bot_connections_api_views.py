# WebexBotConnection model and API were removed. This module is kept so that
# any existing "from bots.webex_bot_connections_api_views import ..." does not
# break. The webex_bot_connections_api_urls module uses empty urlpatterns and
# does not import from here.
